import { StylingDivDirective } from './styling-div.directive';

describe('StylingDivDirective', () => {
  it('should create an instance', () => {
    const directive = new StylingDivDirective();
    expect(directive).toBeTruthy();
  });
});
